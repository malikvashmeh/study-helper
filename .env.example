# Study Mate Bot - Environment Variables (Gemini Only)
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED API KEY
# =============================================================================

# Google API Key (Required for Gemini models and embeddings)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# LLM CONFIGURATION
# =============================================================================

# Default LLM provider: "gemini" (only option)
DEFAULT_LLM=gemini

# Default model to use
# Gemini options: gemini-1.5-flash, gemini-1.5-pro
DEFAULT_MODEL=gemini-1.5-flash

# =============================================================================
# VECTOR DATABASE CONFIGURATION
# =============================================================================

# Vector database type: "faiss" or "chroma"
# FAISS: Faster, good for smaller datasets
# Chroma: More features, better for larger datasets
VECTOR_DB_TYPE=faiss

# Path to store vector database files
VECTOR_DB_PATH=./data/vector_db

# =============================================================================
# EMBEDDING CONFIGURATION
# =============================================================================

# Embedding model to use (Google only)
# Google options: models/embedding-001
EMBEDDING_MODEL=models/embedding-001

# =============================================================================
# DOCUMENT PROCESSING CONFIGURATION
# =============================================================================

# Size of text chunks for document processing
CHUNK_SIZE=1000

# Overlap between chunks (helps maintain context)
CHUNK_OVERLAP=200

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server host and port
BACKEND_HOST=localhost
BACKEND_PORT=8000

# Frontend server port
FRONTEND_PORT=8501

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Maximum file size for uploads (in MB)
MAX_FILE_SIZE_MB=50

# Enable/disable CORS (for production, set to specific origins)
CORS_ORIGINS=*

# =============================================================================
# EXAMPLE CONFIGURATION
# =============================================================================

# For Gemini only (current setup):
# GOOGLE_API_KEY=your-google-key-here
# DEFAULT_LLM=gemini
# DEFAULT_MODEL=gemini-1.5-flash
# VECTOR_DB_TYPE=faiss
# EMBEDDING_MODEL=models/embedding-001

# For production:
# CORS_ORIGINS=http://localhost:8501,https://yourdomain.com
# LOG_LEVEL=WARNING
